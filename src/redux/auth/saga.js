function* loadAuthState() {
  // try {
  //   yield call(
  //     getAuthState,
  //     yield put({type: Types.SCHEDULES_INTERVALS.SET_INTERVALS, intervals}),
  //   );
  // } catch (e) {
  //   console.log(e);
  // }
  //
  // yield put({
  //   type: Types.SET_USER,
  //   // user: user,
  // });
}

export default function* rootSaga() {
  // yield takeEvery(Types.LOAD_AUTH_STATE, loadAuthState);
}
